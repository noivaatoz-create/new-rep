# Database Configuration (REQUIRED for product/order save)
# Vercel: Settings → Environment Variables → DATABASE_URL (paste full URL with real password)
# Replace [YOUR-PASSWORD] with your Supabase database password
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.alvsfygvvwtzfbrcgdpd.supabase.co:5432/postgres

# Session Secret (generate a random string)
SESSION_SECRET=your-super-secret-session-key-change-this

# Node Environment
NODE_ENV=development

# Supabase Storage (image uploads). Products use DATABASE_URL unless you opt in below.
SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key
# Set to true only if your Supabase project has a products table and you want products there.
# USE_DIRECT_SUPABASE_PRODUCTS=true

# Admin Credentials (optional, for initial setup)
ADMIN_USERNAME=adminpokemon
ADMIN_PASSWORD=pokemonadmin

# Timeout overrides (if "Request timed out" on product save)
# Server: how long to wait for DB/storage (default 60000 ms)
STORAGE_PRODUCTS_TIMEOUT_MS=90000
# Server: Supabase product API timeout when USE_DIRECT_SUPABASE_PRODUCTS=true (default 8000 ms)
# SUPABASE_PRODUCTS_TIMEOUT_MS=15000
# Client: product save request timeout in ms (set in .env with VITE_ prefix, default 90000)
# VITE_PRODUCT_SAVE_TIMEOUT_MS=120000
